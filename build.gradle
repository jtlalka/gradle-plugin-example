buildscript {
    apply from: 'gradle/config.gradle'

    repositories {
        mavenCentral()
        google()
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}
